/* CORE CIRCLE */
.cursor-core {
  position: fixed;
  width: 14px;
  height: 14px;
  background: #1e3a8a;
  border-radius: 50%;
  transform: translate(-50%, -50%);
  pointer-events: none;
  z-index: 9999;
  transition: transform 0.15s ease-out, background 0.15s ease-out;
}

/* OUTER GLOW / LAG EFFECT */
.cursor-glow {
  position: fixed;
  width: 40px;
  height: 40px;
  background: rgba(30, 58, 138, 0.15);
  border-radius: 50%;
  transform: translate(-50%, -50%);
  pointer-events: none;
  z-index: 9998;
  transition: all 0.2s ease-out;
  box-shadow:
    0 0 10px rgba(30, 58, 138, 0.3),
    0 0 20px rgba(30, 58, 138, 0.2),
    0 0 30px rgba(30, 58, 138, 0.1);
  backdrop-filter: blur(2px);
}

/* HOVER EFFECT */
.cursor-core.hover {
  transform: translate(-50%, -50%) scale(1.5);
  background: navy-blue;
}

.cursor-glow.hover {
  transform: translate(-50%, -50%) scale(2);
  background: rgba(30, 58, 138, 0.25);
  box-shadow:
    0 0 20px rgba(30, 58, 138, 0.4),
    0 0 30px rgba(30, 58, 138, 0.3),
    0 0 40px rgba(30, 58, 138, 0.2);
}

/* OPTIONAL SMOOTH TRAIL (uses pseudo lag) */
.cursor-glow {
  transition: transform 0.25s ease-out, background 0.2s ease-out;
}
